# .bat 必須是 CRLF 換行。
#
# 為什麼：cmd.exe 逐行執行批次檔時是靠位元組位移回頭讀檔的。檔案若只用 LF 換行、
# 內容又含中文（UTF-8 多位元組），在工作排程器的 cp950 主控台下位移會算錯，
# 於是執行到一行不存在的指令、回傳 1 而且完全不留訊息。
# 實測：同一支 notify.bat，LF 版在排程下回報 1 且 log 一個字都沒寫，
# 改成 CRLF 後回報 0、log 正常。run.bat 當時僥倖沒中，只是位移剛好躲過。
*.bat text eol=crlf

# vbs 沒有同類問題（VBScript 引擎自己 tokenize，實測 LF-only 也正常執行），
# 這條只為了統一格式。注意：不要把 .vbs 存成 UTF-16——這裡宣告了 text，
# git 會把它當純文字做換行轉換而破壞檔案。
*.vbs text eol=crlf
